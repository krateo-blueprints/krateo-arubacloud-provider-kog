# Aruba Cloud Bearer token (see https://api.arubacloud.com/docs/authentication/).
# The token is short-lived; rotate it as needed.
apiVersion: v1
kind: Secret
metadata:
  name: arubacloud-token
  namespace: default
type: Opaque
stringData:
  token: REPLACE_WITH_ARUBA_JWT
